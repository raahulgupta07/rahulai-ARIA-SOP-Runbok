services:
  db:
    image: postgres:17
    container_name: docsensei-db
    # Raise max_connections so DB_POOL_MAX * WEB_CONCURRENCY (+ daemons) fits.
    # Default 100 was tight; 200 covers ~8 workers * 20-conn pools with headroom.
    command: ["postgres", "-c", "max_connections=${PG_MAX_CONNECTIONS:-200}"]
    environment:
      POSTGRES_USER: docsensei
      POSTGRES_PASSWORD: docsensei
      POSTGRES_DB: docsensei
    ports:
      - "5437:5432"
    volumes:
      - docsensei_pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U docsensei -d docsensei"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build:
      context: .
      args:
        BUILD_SHA: ${BUILD_SHA:-local}
        BUILD_DATE: ${BUILD_DATE:-}
    container_name: docsensei-app
    # Multi-worker uvicorn. Background daemons are leader-gated (app/leader.py)
    # so only one worker runs them. ${WEB_CONCURRENCY} workers.
    command: ["sh", "-c", "uvicorn app.main:app --host 0.0.0.0 --port 8077 --workers ${WEB_CONCURRENCY:-4}"]
    depends_on:
      db:
        condition: service_healthy
    environment: &app-environment
      PYTHONUNBUFFERED: "1"
      # 1 = ingest runs inside the API (single-container default). Set 0 in .env and
      # run `docker compose --profile worker up -d` so the dedicated worker ingests
      # instead, keeping heavy vision/PageIndex off the API's GIL.
      INGEST_IN_API: ${INGEST_IN_API:-1}
      # Deferred enrichment: upload runs phase-1 only (instant answerable); the
      # Enrichment Agent runs phase-2 in the background, ENRICH_CONCURRENCY at a time.
      DEFER_ENRICH: ${DEFER_ENRICH:-0}
      ENRICH_CONCURRENCY: ${ENRICH_CONCURRENCY:-2}
      # Eval Agent: nightly offline answer-quality scoring (separate from runtime).
      EVAL_ENABLED: ${EVAL_ENABLED:-0}
      EVAL_HOUR: ${EVAL_HOUR:-2}
      EVAL_CONCURRENCY: ${EVAL_CONCURRENCY:-2}
      EVAL_QUESTIONS_PER_DOC: ${EVAL_QUESTIONS_PER_DOC:-8}
      EVAL_JUDGE_MODEL: ${EVAL_JUDGE_MODEL:-}
      DATABASE_URL: postgresql://docsensei:docsensei@db:5432/docsensei
      DB_POOL_MIN: ${DB_POOL_MIN:-2}
      DB_POOL_MAX: ${DB_POOL_MAX:-20}
      DB_POOL_TIMEOUT: ${DB_POOL_TIMEOUT:-30}
      WEB_CONCURRENCY: ${WEB_CONCURRENCY:-4}
      LLM_MAX_CONCURRENCY: ${LLM_MAX_CONCURRENCY:-15}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY}
      GRAPH_CLIENT_SECRET: ${GRAPH_CLIENT_SECRET:-}
      SHAREPOINT_CLIENT_SECRET: ${SHAREPOINT_CLIENT_SECRET:-}
      SHAREPOINT_SYNC_ENABLED: ${SHAREPOINT_SYNC_ENABLED:-0}
      SHAREPOINT_SYNC_INTERVAL_H: ${SHAREPOINT_SYNC_INTERVAL_H:-6}
      CONNECTOR_SP_ENABLED: ${CONNECTOR_SP_ENABLED:-0}
      INGEST_MODEL: ${INGEST_MODEL:-openrouter/google/gemini-3.1-flash-lite}
      CHAT_MODEL: ${CHAT_MODEL:-google/gemini-3.1-flash-lite}
      API_KEY: ${API_KEY:-}
      RETRIEVE_EXPAND: ${RETRIEVE_EXPAND:-1}
      RETRIEVE_K: ${RETRIEVE_K:-6}
      RETRIEVE_DEPTH: ${RETRIEVE_DEPTH:-5}
      RETRIEVE_RERANK: ${RETRIEVE_RERANK:-1}
      RERANK_POOL: ${RERANK_POOL:-12}
      COMPILE_ON_INGEST: ${COMPILE_ON_INGEST:-0}
      CHAT_USE_WIKI: ${CHAT_USE_WIKI:-0}
      COMPILE_MODEL: ${COMPILE_MODEL:-}
      COMPILE_PLAYBOOK: ${COMPILE_PLAYBOOK:-1}
      PLAYBOOK_MODEL: ${PLAYBOOK_MODEL:-}
      USE_PLAYBOOK_IN_CHAT: ${USE_PLAYBOOK_IN_CHAT:-1}
      TYPE_AWARE_COMPILE: ${TYPE_AWARE_COMPILE:-1}
      ENTITY_INDEX: ${ENTITY_INDEX:-1}
      ENTITY_RETRIEVE: ${ENTITY_RETRIEVE:-1}
      SEMANTIC_CONTRADICTION: ${SEMANTIC_CONTRADICTION:-0}
      CONTRADICTION_MODEL: ${CONTRADICTION_MODEL:-}
      BITEMPORAL: ${BITEMPORAL:-0}
      ENTITY_FILL_MAX: ${ENTITY_FILL_MAX:-3}
      KB_LINT: ${KB_LINT:-1}
      DOC_STALE_DAYS: ${DOC_STALE_DAYS:-365}
      PROC_CHAINING: ${PROC_CHAINING:-1}
      ROLE_DEPTH: ${ROLE_DEPTH:-1}
      MULTIDOC_SYNTH: ${MULTIDOC_SYNTH:-1}
      MULTIDOC_MAX: ${MULTIDOC_MAX:-2}
      TROUBLESHOOT_TREE: ${TROUBLESHOOT_TREE:-1}
      RENDER_DPI: ${RENDER_DPI:-220}
      COMPILE_WORKERS: ${COMPILE_WORKERS:-6}
      ENRICH_WORKERS: ${ENRICH_WORKERS:-7}
      AUTO_CATEGORIZE: ${AUTO_CATEGORIZE:-1}
      VISION_PREREAD: ${VISION_PREREAD:-1}
      # parallel page-vision per doc. 8 cuts single-doc ingest time (vision dominates);
      # lower if ingest starves HTTP under load. Tune per host.
      VISION_WORKERS: ${VISION_WORKERS:-8}
      # ---- auth / runtime ----
      APP_ENV: ${APP_ENV:-dev}
      JWT_SECRET: ${JWT_SECRET:-dev-docsensei-change-me}
      SUPERADMIN_EMAIL: ${SUPERADMIN_EMAIL:-}
      SUPERADMIN_PASSWORD: ${SUPERADMIN_PASSWORD:-}
      CORS_ALLOW_ORIGINS: ${CORS_ALLOW_ORIGINS:-}
      LOGIN_MAX_FAIL: ${LOGIN_MAX_FAIL:-5}
      LOGIN_LOCK_MIN: ${LOGIN_LOCK_MIN:-15}
      # ---- external alerting (warn/error → webhook; empty = off) ----
      ALERT_WEBHOOK_URL: ${ALERT_WEBHOOK_URL:-}
      ALERT_COOLDOWN_SEC: ${ALERT_COOLDOWN_SEC:-300}
      # ---- embeddable widget ----
      EMBED_TOKEN_TTL_MIN: ${EMBED_TOKEN_TTL_MIN:-30}
      EMBED_PRUNE_ENABLED: ${EMBED_PRUNE_ENABLED:-0}
      EMBED_VISITOR_TTL_DAYS: ${EMBED_VISITOR_TTL_DAYS:-90}
      EMBED_EVENT_TTL_DAYS: ${EMBED_EVENT_TTL_DAYS:-60}
      EMBED_PRUNE_INTERVAL_H: ${EMBED_PRUNE_INTERVAL_H:-24}
      # ---- analytics / accuracy daemons (default OFF) ----
      VERIFY_ENABLED: ${VERIFY_ENABLED:-0}
      VERIFY_FLAG_THRESHOLD: ${VERIFY_FLAG_THRESHOLD:-70}
      USAGE_ROLLUP_ENABLED: ${USAGE_ROLLUP_ENABLED:-1}
      # ---- DB retention / vacuum (default OFF) ----
      DB_MAINTENANCE_ENABLED: ${DB_MAINTENANCE_ENABLED:-0}
      DB_SESSION_TTL_DAYS: ${DB_SESSION_TTL_DAYS:-30}
      DB_MAINTENANCE_INTERVAL_H: ${DB_MAINTENANCE_INTERVAL_H:-24}
      # ---- auto-learning Layer B (default OFF) ----
      AUTO_LEARN_ENABLED: ${AUTO_LEARN_ENABLED:-1}
      AUTO_LEARN_MODE: ${AUTO_LEARN_MODE:-per_answer}
      AUTO_LEARN_MIN_CONF: ${AUTO_LEARN_MIN_CONF:-0.7}
      AUTO_APPROVE_MIN_CONF: ${AUTO_APPROVE_MIN_CONF:-0.95}
      AUTO_LEARN_MAX_PER_CONV: ${AUTO_LEARN_MAX_PER_CONV:-5}
      AUTO_FACTS_ENABLED: ${AUTO_FACTS_ENABLED:-1}
      AUTO_FACTS_MAX: ${AUTO_FACTS_MAX:-6}
      # ---- auto Q&A pairs from documents (default ON) ----
      AUTO_QA_ENABLED: ${AUTO_QA_ENABLED:-1}
      AUTO_QA_MAX: ${AUTO_QA_MAX:-8}
      AUTO_QA_HARVEST: ${AUTO_QA_HARVEST:-1}
      # ---- Phase 3: gap-driven Q&A daemon (default OFF — costs LLM) ----
      AUTO_QA_GAP_ENABLED: ${AUTO_QA_GAP_ENABLED:-0}
      AUTO_QA_GAP_PER_DAY: ${AUTO_QA_GAP_PER_DAY:-5}
      AUTO_QA_GAP_INTERVAL_H: ${AUTO_QA_GAP_INTERVAL_H:-24}
      # ---- Sector-based row-level access (multi-tenant; default OFF = single-tenant) ----
      RBAC_ENABLED: ${RBAC_ENABLED:-0}
      # ---- GraphRAG-A: typed entity relationships + multi-hop walk (default OFF — +1 LLM/doc at ingest) ----
      GRAPHRAG_ENABLED: ${GRAPHRAG_ENABLED:-0}
      # ---- per-image detailed screenshot explanation (default OFF — vision per image) ----
      DOC_IMAGES_ENABLED: ${DOC_IMAGES_ENABLED:-0}
      GRAPHWALK_HOPS: ${GRAPHWALK_HOPS:-2}
      GRAPHRAG_MAX_DOCS: ${GRAPHRAG_MAX_DOCS:-4}
      # ---- Self-Heal agent: parallel per-doc eval-gated self-improvement (default OFF — costs LLM) ----
      SELFHEAL_ENABLED: ${SELFHEAL_ENABLED:-0}
      SELFHEAL_MAX_ROUNDS: ${SELFHEAL_MAX_ROUNDS:-3}
      SELFHEAL_JUDGES: ${SELFHEAL_JUDGES:-3}
      SELFHEAL_POLL_MIN: ${SELFHEAL_POLL_MIN:-5}
      # ---- Phase 4: serve cached answers from the Q&A bank (default ON) ----
      AUTO_QA_SERVE_ENABLED: ${AUTO_QA_SERVE_ENABLED:-1}
      AUTO_QA_SERVE_MIN_SIM: ${AUTO_QA_SERVE_MIN_SIM:-0.72}
      # ---- auto complexity routing ----
      AUTO_ROUTE_ENABLED: ${AUTO_ROUTE_ENABLED:-1}
      AUTO_ROUTE_LLM: ${AUTO_ROUTE_LLM:-1}
      # ---- agentic iterative retrieval ----
      AGENTIC_RETRIEVE_ENABLED: ${AGENTIC_RETRIEVE_ENABLED:-1}
      AGENTIC_MAX_SUBS: ${AGENTIC_MAX_SUBS:-3}
      # ---- AI admin/ops agent ----
      OPS_AGENT_ENABLED: ${OPS_AGENT_ENABLED:-1}
      # ---- shareable answer links ----
      SHARE_ENABLED: ${SHARE_ENABLED:-1}
      SHARE_PUBLIC_ENABLED: ${SHARE_PUBLIC_ENABLED:-0}
      DATA_DIR: ./data
      PAGES_DIR: ./data/pages
      UPLOADS_DIR: ./data/uploads
      IMPORT_DIR: ${IMPORT_DIR:-./documents}   # "Import from server" scans this folder
      # ---- S3 / MinIO object storage (durable backend + bulk import source) ----
      STORAGE: ${STORAGE:-local}               # local | s3
      S3_BUCKET: ${S3_BUCKET:-}
      S3_REGION: ${S3_REGION:-us-east-1}
      S3_ENDPOINT_URL: ${S3_ENDPOINT_URL:-}    # e.g. http://minio:9000 for MinIO
      S3_ACCESS_KEY_ID: ${S3_ACCESS_KEY_ID:-}
      S3_SECRET_ACCESS_KEY: ${S3_SECRET_ACCESS_KEY:-}
      S3_PREFIX: ${S3_PREFIX:-docsensei/}
      S3_IMPORT_PREFIX: ${S3_IMPORT_PREFIX:-inbox-drop/}
      S3_FORCE_PATH_STYLE: ${S3_FORCE_PATH_STYLE:-1}
    ports:
      - "8082:8077"
    extra_hosts:
      # lets the app container reach the throwaway Keycloak on the SAME url the
      # browser uses (kc.local:8088 → host published port). Split-horizon fix.
      - "kc.local:host-gateway"
    volumes:
      - ./data:/app/data        # page images + uploads persist on host
      - ./documents:/app/documents   # drop SOPs here → "Import from server" queues them

  # Dedicated ingest worker (opt-in: `--profile worker`). Same image, no HTTP —
  # runs the ingest worker + daemons so vision/PageIndex never share the API GIL.
  # To use: set INGEST_IN_API=0 in .env, then `docker compose --profile worker up -d`.
  worker:
    profiles: ["worker"]
    build:
      context: .
      args:
        BUILD_SHA: ${BUILD_SHA:-local}
        BUILD_DATE: ${BUILD_DATE:-}
    image: cityagentdocsensei-app
    container_name: docsensei-worker
    command: ["python", "-m", "app.worker_main"]
    depends_on:
      db:
        condition: service_healthy
    environment:
      <<: *app-environment
      # isolated process → push vision concurrency higher than the API would dare
      VISION_WORKERS: ${WORKER_VISION_WORKERS:-12}
    volumes:
      - ./data:/app/data
      - ./documents:/app/documents

volumes:
  docsensei_pg:
